//! Composition root reserved for the CLI and optional HTTP boundary.
#![forbid(unsafe_code)]

fn main() {
    eprintln!("Ledger Lab: Phase 0 scaffold only; no application commands implemented.");
    std::process::exit(2);
}
