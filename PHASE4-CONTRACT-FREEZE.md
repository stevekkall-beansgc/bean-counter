# Phase 4 additive contract freeze

The owner approved an additive freeze of exact canonical candidate `c8fbddc682e22e10c6122abbcf4c8212a1c407fa` and continuation of the remaining Phase4 requirements. The separate registration is `contracts/freezes/central-adjudication-r3-1.json`, profile `central-adjudication-r3/1`, status **frozen-unreleased**.

All91 reviewed candidate files remain at their original paths and byte-identical. Their historical candidate/unfrozen wording is retained; this additive registration establishes current status. Earlier freeze registries, schemas, validators, fixtures and the existing scripts/check.sh are unchanged. The independently accepted PostgreSQL baseline-test correction is adopted separately in the integration lineage.

The manifest pins the complete candidate (including ARTIFACTS.json), exact R3 design, independent four-blocker closure, owner authorization, this overlay and the separate read-only checker. Run `sh scripts/check-phase4-contract.sh` to verify it. The full reference validation remains the existing candidate check.py with an external output directory and cached Python/Node environment. Its existing exact-source independent pass is retained, not gratuitously rerun for metadata-only freezing.

This is a contract freeze, not runtime/store/gateway acceptance. Next is one concrete typed-interface review, followed by required actual implementation and G2 evidence on an assembled build. The full R3 design and independent oracle remain normative; physical quota/workspace, writer exclusion, crash/unknown-commit, trusted reads and observed nonposting cannot be established by reference-model agreement.

Any necessary contract correction requires an explicit additive amendment and fresh exact-source independent review; no silent frozen-byte edits. Ordinary implementation choices within the owner-approved Phase4 scope are authorized. No new product policy, Phase5/6, main merge, release/deployment, destructive cleanup or infrastructure/spend is authorized.
